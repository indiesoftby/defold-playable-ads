go.property("receiver", msg.url("/main"))
go.property("message_id", hash("FILL_IT"))

function init(self)
	msg.post("#sprite", "disable")
	
	msg.post(self.receiver, self.message_id, {
		pos = go.get_position()
	})
end
